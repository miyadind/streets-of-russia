name: Build Price Translator APK

on:
  push:
    branches: [price-translator-build]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android platform
        run: sdkmanager "platforms;android-36" "build-tools;36.0.0"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.11.1'

      - name: Build debug APK
        working-directory: price-translator
        run: gradle assembleDebug --stacktrace

      - name: Rename APK
        run: cp price-translator/app/build/outputs/apk/debug/app-debug.apk PriceTranslator.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: PriceTranslator-APK
          path: PriceTranslator.apk
          if-no-files-found: error
